# 🚀 دليل الاستخدام السريع - النظام الجديد

## للمدرس 👨‍🏫

### 📝 رفع محاضرة جديدة:
1. اذهب إلى **رفع المحاضرات** من القائمة
2. اختر **الكورس**
3. **✨ الجديد**: اختر **الصف الدراسي** (الأول الثانوي، الثاني الثانوي، الثالث الثانوي)
4. اختر **المجموعة**
5. أدخل باقي البيانات ورفع المحاضرة

### ✅ إنشاء امتحان جديد:
1. اذهب إلى **إنشاء امتحانات** من القائمة
2. اختر **الكورس**
3. **✨ الجديد**: اختر **الصف الدراسي** (إجباري)
4. أدخل باقي البيانات (العنوان، المدة، الأسئلة، إلخ)
5. أنشئ الامتحان

### 💡 نصيحة:
- إذا كان الامتحان **لصف معين** → اختر الصف
- إذا كان الامتحان **لجميع الصفوف** → لا تختر صف (سيظهر للجميع)

---

## للطالب 🎓

### 🏠 الصفحة الرئيسية (Dashboard):
عند فتح الحساب، ستجد 4 بطاقات ملونة:

1. **🔵 الامتحانات**
   - عدد الامتحانات المتاحة لك
   - اضغط عليها للانتقال لصفحة الامتحانات

2. **🟣 المحاضرات**
   - اضغط عليها لمشاهدة المحاضرات الخاصة بصفك

3. **🟢 النتائج**
   - عدد نتائج الامتحانات الخاصة بك
   - اضغط عليها لرؤية نتائجك

4. **🟠 المحتوى التعليمي**
   - عدد الملفات المتاحة
   - اضغط عليها لتحميل الملفات

### ✨ الجديد:
- **لا مزيد من قائمة "الكورسيات المسجلة"** - تم استبدالها بنظام أسرع وأسهل
- **الامتحانات والمحاضرات مفلترة تلقائياً** حسب صفك الدراسي
- **لن ترى محتوى الصفوف الأخرى** - فقط المحتوى الخاص بصفك

---

## 📊 لوحة التحكم (Admin)

### ✅ التحقق من البيانات:
```bash
# تشغيل سكريبت التحقق من المحاضرات
node check-student-lectures.js

# تشغيل سكريبت التحقق من الامتحانات
node check-student-exams.js  # (إذا كان موجوداً)
```

### 🔧 إضافة grade_id يدوياً للامتحانات الموجودة:
```sql
-- مثال: تحديث امتحان معين للصف الأول الثانوي
UPDATE exams 
SET grade_id = 'grade-id-here' 
WHERE title = 'اسم الامتحان';

-- معرفة IDs الصفوف
SELECT id, name FROM grades;
```

---

## ❓ الأسئلة الشائعة

### س: ماذا لو لم يختر المدرس صف عند رفع امتحان؟
**ج:** الامتحان سيظهر لجميع الطلاب في جميع الصفوف.

### س: هل يمكن للطالب رؤية محاضرات صف آخر؟
**ج:** لا، كل طالب يرى فقط محاضرات صفه أو محاضرات مجموعته.

### س: ماذا عن الامتحانات القديمة؟
**ج:** الامتحانات القديمة (قبل التحديث) لها `grade_id = NULL` وستظهر لجميع الطلاب.

### س: كيف أعرف صف الطالب؟
**ج:** من جدول `students` → عمود `grade_id` و `grade` (الاسم).

---

## 🛠️ استكشاف الأخطاء

### مشكلة: الطالب لا يرى الامتحانات
**الحل:**
1. تأكد من أن المدرس اختار صف الطالب عند إنشاء الامتحان
2. تأكد من أن الطالب لديه `grade_id` في جدول `students`
3. تأكد من أن الامتحان `is_active = TRUE`

### مشكلة: المدرس لا يرى قائمة الصفوف
**الحل:**
1. تأكد من وجود صفوف في جدول `grades`
2. تحقق من أن دالة `loadGrades()` تعمل بشكل صحيح
3. تحقق من Console للأخطاء

---

## 📞 الدعم الفني

إذا واجهت أي مشكلة:
1. تحقق من Console (F12) للأخطاء
2. تأكد من أن Backend يعمل (`npm run dev` في مجلد server)
3. تحقق من اتصال قاعدة البيانات
4. راجع ملف `STUDENT_DASHBOARD_GRADE_FILTER_COMPLETE.md` للتفاصيل التقنية

---

**آخر تحديث**: January 23, 2026  
**الإصدار**: 2.0  
**الحالة**: ✅ جاهز للاستخدام
